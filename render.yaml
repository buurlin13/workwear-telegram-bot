services:
  - type: web
    name: workwear-bot
    env: python
    runtime: python
    plan: free
    region: frankfurt
    buildCommand: ""
    startCommand: "python bot.py"
    envVars:
      - key: BOT_TOKEN
        value: your_real_bot_token_here
